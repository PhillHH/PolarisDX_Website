# PolarisDX Relaunch — Branch Reconciliation Map

> Read-only analysis produced 2026-08-21 in `/home/phillip/01polaris-preview`.
> No branch was checked out, no file staged, committed, merged, cherry-picked or modified.
> Every inspection used `git show` / `git log` / `git diff` / `git cherry` / `git ls-tree` / `git grep <ref>` against refs.
> HEAD was not changed. No environment-variable values or secrets appear below.

---

## 1. Executive Summary

**Locked baseline:** `feat/home-leadmagnet@961f65d456e2790e7063d1a6575651dff724e4ca` (2026-08-18 12:40).

**Inspected source refs:**

| Ref                   | SHA                                        | Date             | Role                                     |
| --------------------- | ------------------------------------------ | ---------------- | ---------------------------------------- |
| `main`                | `d0fdf29cb1dbde78cc743b4d7a5077b79c6dafaf` | 2026-08-19 17:02 | selective source A                       |
| `redesign/preview`    | `5673b611de5225c52fd304c874389c58dee85a14` | 2026-06-26 13:27 | selective source B (engineering/QA only) |
| `feat/contact-joyful` | `ab373a38b6935d1419ab512c9ae7274b1013b11e` | 2026-07-03 12:56 | selective source C (optional)            |

**Overall recommendation: selective, hunk-level reimplementation — not merging, not branch-level cherry-picking.**

The decisive measured fact that shapes this entire map:

> **`main` is newer only in the Epigenetics feature layer. On every piece of shared infrastructure it is OLDER than the locked baseline.**

`git diff --name-status 961f65d d0fdf29` reports **31 files that exist on the baseline and not on `main`**, including the whole Sales-Machine section set (`IglooProHero.tsx`, `IglooFeaturesSection.tsx`, `IglooSpecsSection.tsx`, `IglooParametersSection.tsx`, `IglooProductFinalCta.tsx`, `DiagnosticsHero.tsx`, `DiagnosticsFocusSection.tsx`, `DiagnosticsSpecialtySection.tsx`, `TrustBar.tsx`, `SubpageHero.tsx`, `FinalCtaSection.tsx`, `PageSidebar.tsx`, `PraxisOrderForm.tsx`, `RoiCalculatorSection.tsx`, `StepsSection.tsx`, `WhyPocSection.tsx`, `LegalLayout.tsx`), four QA scripts, and `src/lib/articleMeta.ts`. `git diff 961f65d d0fdf29 -- server.ts` shows `main` **lacks** the baseline's real-HTTP-404 machinery, legacy redirects, German-only handling, `no-store` HTML caching, robots-meta de-duplication, font preload and the `127.0.0.1` bind.

Consequently: **no whole file from `main` that also exists on the baseline may be imported.** Only genuinely _new_ files and precisely delimited hunks qualify.

**Candidate counts**

| Status                                                     | Count |
| ---------------------------------------------------------- | ----- |
| IMPORT                                                     | 10    |
| REIMPLEMENT                                                | 15    |
| REFERENCE_ONLY                                             | 7     |
| DEFER                                                      | 4     |
| DO_NOT_IMPORT                                              | 24    |
| _(ALREADY_PRESENT — recorded but outside the five counts)_ | 2     |

---

## 2. Authority and Decision Locks

**`feat/home-leadmagnet@961f65d` remains the baseline. This document is an import map, not a baseline-selection exercise.**

The prior repository baseline analysis (`REPO-BASELINE-2026-08-21.md`, §11) recommended `main@d0fdf29` as a technically newer baseline. **That recommendation is explicitly superseded and not applied here.** The Product Owner / final Master Scope locks the baseline; that decision is closed. Everything below asks only one question: _which individual changes should later travel INTO `961f65d`?_

The evidence gathered for this map, incidentally, strengthens the lock rather than weakening it — see §11, where `main`'s versions of `server.ts`, `SEOHead.tsx`, `index.html`, `tailwind.config.js`, `Header.tsx`, `CookieBanner.tsx` and `lib/tracking.ts` are all measurably behind the baseline.

**Decision Locks applied as filters throughout:**

1. Sales-Machine (baseline) is the visual/art-direction baseline.
2. Main site stays Light. No alternative redesign art direction. No dark-theme work.
3. Epigenetics is an independent business pillar.
4. Consumer landing pages stay indexable; all 10 languages eventually.
5. No Chat / HiHuman in the relaunch; `/api/chat` and chat remnants removed later.
6. IglooPro `CV < 2 %` remains.
7. General enquiry CTA = "Angebot anfragen".
8. The site-wide "garantierte Performance" CTA band must NOT return.
9. Deal / Voucher / Case Studies / Shop are deferred.
10. GTM/GA4 must eventually load only after consent.
11. The baseline's hardened SSR / 404 / SEO / cache behaviour must not regress.

Any candidate contradicting one of these is classified `DO_NOT_IMPORT` regardless of how new or complete it looks.

---

## 3. Repository / Worktree Constraints

| Fact                                                                                                                                                                        | Evidence                                                                                                                                                                                                     |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Working directory is a **git worktree** sharing objects/refs with 27 others                                                                                                 | `git rev-parse --git-common-dir` → `/home/phillip/01polaris/.git`; `git worktree list` → 28 entries                                                                                                          |
| `main` is checked out in `/home/phillip/01polaris` and **cannot be checked out here**                                                                                       | `git worktree list` line `… /home/phillip/01polaris  d0fdf29 [main]`                                                                                                                                         |
| HEAD is `feat/home-leadmagnet@961f65d`; **HEAD was not changed by this analysis**                                                                                           | `git rev-parse HEAD` before and after                                                                                                                                                                        |
| Working tree is **dirty**: `src/pages/EpigeneticsPage.tsx` modified                                                                                                         | `git status --porcelain` → ` M src/pages/EpigeneticsPage.tsx`                                                                                                                                                |
| That modification is a **partial replay of `main@e21a6e5`** — the `<th>` half without the wrapper `overflow-x-auto` removal that `e21a6e5`'s message documents as mandatory | `git show e21a6e5 -- src/pages/EpigeneticsPage.tsx`; working tree line [src/pages/EpigeneticsPage.tsx:401](src/pages/EpigeneticsPage.tsx#L401) still reads `className="mt-10 overflow-x-auto rounded-3xl …"` |
| **This working-tree change is NOT part of the canonical baseline** and was neither modified nor discarded by this task                                                      | per task instruction §3                                                                                                                                                                                      |
| `projektverzeichnis/` (11 files) is untracked; not touched                                                                                                                  | `git status --porcelain` → 11 `??` entries                                                                                                                                                                   |
| Stash depth 0; no merge/rebase/cherry-pick in progress                                                                                                                      | `git stash list \| wc -l`; no `MERGE_HEAD`/`REBASE_*`/`CHERRY_PICK_HEAD` in the worktree git dir                                                                                                             |
| All four refs share exactly one merge base: **`ae58074`** (2026-06-24)                                                                                                      | `git merge-base` for all six pairings returns `ae58074`                                                                                                                                                      |
| `main` ↔ baseline divergence: 77 / 120 commits, **312 files, +24 004 / −13 950**                                                                                            | `git rev-list --left-right --count 961f65d...d0fdf29`; `git diff --stat 961f65d d0fdf29`                                                                                                                     |
| `git cherry d0fdf29 961f65d` → **108 `+` / 12 `−`**                                                                                                                         | 108 baseline commits have no patch-equivalent on `main`                                                                                                                                                      |
| Neither `feat/home-leadmagnet` nor `feat/contact-joyful` exists on `origin`                                                                                                 | `git branch -r --list origin/feat/home-leadmagnet` → empty; same for `feat/contact-joyful`                                                                                                                   |
| Remote-tracking refs reflect the last fetch, **2026-08-19 14:36**                                                                                                           | `stat .git/FETCH_HEAD`                                                                                                                                                                                       |

**Operational consequence:** every import below must be performed as an edit against the baseline working tree (or a branch off it), never as `git merge` / `git cherry-pick` from `main`.

---

## 4. main@d0fdf29 — Positive Candidate Map

| Candidate                                                                    | Commit(s)                                            | Files                                                                                                                                                | Status             | AP          | Risk                                                        | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ----------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A1** Epigenetics tokens module                                             | `b699435`, `543fe41`                                 | `src/components/epigenetics/tokens.ts` (new, 109 lines)                                                                                              | **IMPORT**         | AP15        | LOW                                                         | Zero imports — pure constants (`BODY`, `LEAD`, `LABEL`, `STRETCH`, `ASSET_BASE`), `asArray<T>()`, the `VERTIEFUNGEN` table and `metaDescription()` (`META_MIN` 120 / `META_MAX` 158, sentence-boundary truncation). File does not exist on the baseline (`git ls-tree 961f65d src/components/epigenetics/` → empty). Nothing in it touches layout or art direction.                                                                                                                                                               |
| **A2** `EpiSubpage` shared frame                                             | `b699435` (121 lines), `543fe41` (+119)              | `src/components/epigenetics/EpiSubpage.tsx` (new, 208 lines)                                                                                         | **REIMPLEMENT**    | AP15        | MEDIUM                                                      | Behaviour is wanted and its i18n contract is fully satisfied by the baseline (see §6.1). **But it imports `trackEvent` from `../../lib/tracking`, which does not exist on the baseline** — the baseline exports `track(ereignis)` plus `setTrackingProvider`/`setTrackingConsent` (`git show 961f65d:src/lib/tracking.ts \| grep ^export`), while `main` exports only `trackEvent(event, params)`. It also uses `text-text-heading`, a token present only in `main`'s `tailwind.config.js`. Two adaptations ⇒ not a literal copy. |
| **A3** Three depth pages                                                     | `b699435`, `543fe41`                                 | `src/pages/EpigeneticsBasicsPage.tsx`, `EpigeneticsEvidencePage.tsx`, `EpigeneticsDocsPage.tsx` (all new)                                            | **REIMPLEMENT**    | AP15        | MEDIUM                                                      | Same `trackEvent` dependency (`git grep -ln lib/tracking d0fdf29` lists `EpigeneticsDocsPage.tsx` and `EpigeneticsEvidencePage.tsx`). Content is assembled purely from existing `epigenetics` locale keys — no new translation round (documented in `EpiSubpage.tsx` header).                                                                                                                                                                                                                                                     |
| **A4** Routes for the three depth pages                                      | `b699435` (+30 in App.tsx), `543fe41` (+27)          | `src/App.tsx` (hunk only)                                                                                                                            | **REIMPLEMENT**    | AP10        | MEDIUM                                                      | The three `<Route path="/epigenetics/{grundlagen,studienlage,unterlagen}">` blocks plus their three `lazy()` imports. **Do not import `src/App.tsx` wholesale** — the same file on `main` deletes `GermanOnlyPage` (§5, N12).                                                                                                                                                                                                                                                                                                     |
| **A5** Six Musterbefund route modules                                        | `d0fdf29`                                            | `src/pages/musterbefund/{metabolic-health,healthy-aging,biologische-altersuhr,telomer-analyse,stress-monitor,healthy-sport}.tsx` (6 × 20 lines, new) | **IMPORT**         | AP16 / AP25 | LOW                                                         | Each imports exactly its own two JSON files, `type Befund` from `content/befunde/meta`, and `MusterbefundPage`. Purpose measured in the commit: a shared module pulled all six panels in both languages into one 287 KB chunk to display 24 KB. **Precondition: A6 and A7.**                                                                                                                                                                                                                                                      |
| **A6** `content/befunde/meta.ts`                                             | `d0fdf29` (+51)                                      | `src/content/befunde/meta.ts` (new)                                                                                                                  | **IMPORT**         | AP16        | LOW                                                         | JSON-free metadata module: `Befund`, `BefundSprachen`, `BEFUND_ORDER`, `RADAR_VALUES`. Explicitly documented as the file that must stay free of JSON imports. Baseline has the same constants embedded in `src/content/befunde/index.ts` alongside all twelve JSON imports (322 KB).                                                                                                                                                                                                                                              |
| **A7** `befunde/index.ts` re-export split + `MusterbefundPage` prop refactor | `d0fdf29` (index.ts −48/+? , MusterbefundPage +181)  | `src/content/befunde/index.ts`, `src/pages/MusterbefundPage.tsx`                                                                                     | **REIMPLEMENT**    | AP16 / AP25 | HIGH                                                        | The prop-based refactor (`befunde?: BefundSprachen`, explicit `slug` prop) is the enabling change for A5. **But the same file on `main` also drops `LanguageFallbackNotice` and the `ArrowUp`/`befund.toTop` control** — both present on the baseline (§5, N13). Reproduce the props + chunk split only.                                                                                                                                                                                                                          |
| **A8** `ChapterNav` action callback + anchor marker                          | `543fe41` (+19)                                      | `src/components/ui/ChapterNav.tsx` (3 small hunks)                                                                                                   | **IMPORT**         | AP06 / AP24 | LOW                                                         | (a) `onClick?: () => void` added to `NavAction`; (b) `data-chapterbar=""` on the sticky wrapper; (c) `<span className="sr-only">{switcher.label}: </span>` in the panel switcher trigger. Files are otherwise near-identical (both export the same `Chapter`, `NavAction`, `ChapterNavProps`). (a) is a hard dependency of A2. **Do not take the `text-heading` → `text-text-heading` rename in the same file** (§5, N4).                                                                                                         |
| **A9** Anchor-jump guard for chapter-bar pages                               | `d0fdf29` lineage (`src/App.tsx` `ScrollToHash`)     | `src/App.tsx` (one hunk)                                                                                                                             | **IMPORT**         | AP10 / AP24 | LOW                                                         | When a chapter bar exists in the document but `--chapterbar-offset` has not been written yet, wait a frame instead of computing a wrong offset. Measured in the diff comment: `/de/epigenetics/grundlagen#prinzip` landed at 106 px instead of 161 px — 55 px behind the bar — on **direct URL entry** (not on chip clicks). Pairs with A8(b).                                                                                                                                                                                    |
| **A10** Three sitemap entries for the depth pages                            | `b699435` (`server.ts` +10)                          | `server.ts` — `SITEMAP_ROUTES` only                                                                                                                  | **REIMPLEMENT**    | AP09 / AP10 | LOW _(as three lines)_ / **CRITICAL if the file is copied** | Add `{ path: '/epigenetics/grundlagen' \| '/studienlage' \| '/unterlagen', priority: 0.6, changefreq: 'monthly' }`. Because the baseline derives `KNOWN_PATHS` from `SITEMAP_ROUTES`, these three lines simultaneously fix the real-404 table. **`server.ts` must never be imported from `main`** (§5, N1).                                                                                                                                                                                                                       |
| **A11** `createItemListSchema`                                               | `86fd38d` (+40 in structuredData.ts, +1 in index.ts) | `src/components/seo/structuredData.ts`, `src/components/seo/index.ts`                                                                                | **IMPORT**         | AP09        | LOW                                                         | Purely additive `ItemList` generator for the six analyses (`numberOfItems`, `itemListOrder`, `ListItem` with `canonicalUrlFor`). Deliberately price-free — matches the "no prices" rule of the Epigenetics track. Add the function and the barrel export; **do not** take the rest of `main`'s `structuredData.ts` (§5, N14).                                                                                                                                                                                                     |
| **A12** Dual origin contract in the contact form                             | `05a58f2` (+24 in ContactForm.tsx)                   | `src/components/sections/ContactForm.tsx` (one hunk)                                                                                                 | **REIMPLEMENT**    | AP15        | MEDIUM                                                      | Baseline recognises only `source=epigenetics` (`git show 961f65d:…ContactForm.tsx` line 74). `main` accepts `params.get('topic') === 'epigenetik' \|\| params.get('source') === 'epigenetics'` (line 51). Required if A13 is taken. The rest of `main`'s ContactForm is 646 lines of churn against a baseline that already has `aria-invalid` / `aria-describedby` / `role="alert"` / focus restoration — reproduce the one condition, nothing else.                                                                              |
| **A13** Header contact target on the Epigenetics track                       | `05a58f2` (+23 in Header.tsx)                        | `src/components/layout/Header.tsx` (one hunk)                                                                                                        | **REIMPLEMENT**    | AP06 / AP15 | HIGH _(file)_ / LOW _(hunk)_                                | `kontaktZiel` sends the permanent header contact button to `/contact?topic=epigenetik#kontaktformular` while on `/epigenetics*`, instead of dropping the reader into the IglooPro questionnaire. **`Header.tsx` must not be imported wholesale** (§5, N11).                                                                                                                                                                                                                                                                       |
| **A14** Enquiry origin in the notification e-mail subject                    | `05a58f2` (+45 in server/server.js)                  | `server/server.js` (one hunk)                                                                                                                        | **REIMPLEMENT**    | AP15        | MEDIUM                                                      | Extracts the trailing `\n\nHerkunft: …` line the form appends, lifts it to the top of the mail and prefixes the subject with `[Epigenetik]`. Self-contained and German-internal (no i18n). `main`'s `server/server.js` is otherwise −187/+43 against the baseline, so take the hunk only.                                                                                                                                                                                                                                         |
| **A15** Phone-number unification                                             | `475fefd`                                            | 10 × `public/locales/*/vitd3spray.json`, `src/components/seo/structuredData.ts`, 12 × `src/content/befunde/*.json`, `email/s3-leitlinie.html`        | **REIMPLEMENT**    | AP08 / AP09 | LOW                                                         | Measured on the baseline: **three distinct numbers in circulation** — `+49 152 2858 0999` (13×), `+49 151 75011699` (10× plus 5× unspaced), `+49 1515 9878599` (1×) (`git grep -ohE '\+49[ 0-9]{6,}' 961f65d`). `main` has exactly one (27× plus 5× unspaced). This is a data decision, not a code import — apply the target number across the baseline's own (larger) file set.                                                                                                                                                  |
| **A16** Value-count and disclosure corrections on Musterbefund pages         | `d0fdf29`                                            | `src/pages/MusterbefundPage.tsx` (two hunks)                                                                                                         | **REIMPLEMENT**    | AP16        | LOW                                                         | (a) `bigResult` added to `ALWAYS_OPEN` — on Telomer-Analyse and biologische Altersuhr the headline result was collapsed on exactly the two panels that also lacked an overview. (b) `ZAEHLBAR` set restricts the "n Werte" counter to real measurement blocks and suppresses it at n = 1 (the key `befund.entryCount` has no singular form, so it rendered "1 Werte").                                                                                                                                                            |
| **A17** Umbaukonzept + content strategy                                      | `b699435`, `031f71f`                                 | `_project-knowledge/epigenetik-umbaukonzept.md` (310 lines), `_project-knowledge/epigenetik-content-strategie.md` (224 lines)                        | **REFERENCE_ONLY** | AP15        | —                                                           | Design rationale for the three-page split and the panel funnel. Valuable as evidence for the implementing agent; must not become tracked project documentation that competes with `projektverzeichnis/`.                                                                                                                                                                                                                                                                                                                          |
| **A18** Chat removal recipe                                                  | `main` lineage                                       | `src/App.tsx` (import + `<ChatWidget />` in `MainLayout`), `src/components/ui/ChatWidget.tsx` (deleted on `main`)                                    | **REFERENCE_ONLY** | AP23        | —                                                           | `main` has already removed the widget (`git grep -l ChatWidget d0fdf29 -- src/` → empty; baseline still lists `src/App.tsx` and `src/components/ui/ChatWidget.tsx`). Use as the exact removal recipe when AP23 runs — but note `POST /api/chat` still exists on **both** branches (`server/server.js:503` baseline, `:521` main), so `main` is _not_ a complete precedent.                                                                                                                                                        |
| **A19** CSP without HiHuman domains                                          | `main` lineage (`server.ts` CSP block)               | `server.ts` — `cspReportOnly` array                                                                                                                  | **DEFER**          | AP23        | LOW                                                         | `main` drops `https://widget.hihuman.co.uk` / `https://*.hihuman.co.uk` from `script-src`, `connect-src` and `frame-src`. Correct direction, but it belongs with the chat removal, not before it. Apply as three token deletions in the baseline's own CSP string — never by importing `server.ts`.                                                                                                                                                                                                                               |
| **A20** Event instrumentation on Merkliste / BefundBlocks / Panels           | `c59cf5d`, `05a58f2`, `86fd38d`                      | `src/components/befund/Merkliste.tsx`, `BefundBlocks.tsx`, `src/components/sections/EpigeneticsPanels.tsx`                                           | **DEFER**          | AP23        | MEDIUM                                                      | Behaviourally desirable (all four enquiry paths become measurable) but written against `main`'s direct-`dataLayer` `trackEvent`. Must wait until the consent architecture decides the final event API on top of the baseline's provider-neutral `track()`.                                                                                                                                                                                                                                                                        |
| **A21** `EpigeneticsPanels.tsx` / `EpigeneticsTeaserSection.tsx` reworks     | `b699435`, `86fd38d`                                 | both files (exist on both refs)                                                                                                                      | **DEFER**          | AP15        | MEDIUM                                                      | Both files exist on the baseline **and** on `main` and were changed on both lines independently. A per-hunk comparison was not completed in this pass — see §17, gap G3.                                                                                                                                                                                                                                                                                                                                                          |

---

## 5. main@d0fdf29 — Explicit Do-Not-Import Map

| #       | Item                                          | Commit(s)                                | Files                                                                                                                                  | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Coupled to useful work?                                                                                                                                                                                                                                           |
| ------- | --------------------------------------------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **N1**  | `server.ts` in any form                       | whole `main` line                        | `server.ts`                                                                                                                            | `git diff 961f65d d0fdf29 -- server.ts` shows `main` **lacks**: `isKnownPath`/`KNOWN_PATHS`/`EXTRA_KNOWN_PATHS` and the `NOT_FOUND_MARKER` soft-404 detector (⇒ unknown URLs answer **HTTP 200**), `LEGACY_PATH_REDIRECTS` (`/agb`, `/s3-leitlinie`), `GERMAN_ONLY_PATHS` + its 301 middleware + `GERMAN_ONLY_SITEMAP_ROUTES`, the robots/googlebot duplicate-meta strip, `Cache-Control: no-store` on HTML, `getFontPreloadTag()`, and the `127.0.0.1` listen bind. Importing it regresses Locks 11 and 4 at once. | **YES** — A10 (three sitemap lines) lives inside this file. Take the three lines by hand.                                                                                                                                                                         |
| **N2**  | `SEOHead.tsx`                                 | `main` line                              | `src/components/seo/SEOHead.tsx` (−63/+21)                                                                                             | `main` deletes the `notFound` prop (which emits `<meta name="prerender-status-code" content="404">`, the signal `server.ts` reads), deletes `GERMAN_ONLY_PATHS`, and makes the canonical always follow the current language. Direct SEO regression.                                                                                                                                                                                                                                                                 | No                                                                                                                                                                                                                                                                |
| **N3**  | `index.html`                                  | `main` line                              | `index.html`                                                                                                                           | `main` **re-adds** the global German `meta name="keywords"` block that the baseline removed (it shipped on every page of every language, e.g. `/en/terms`) and removes the comment contract behind the robots-fallback de-duplication.                                                                                                                                                                                                                                                                              | No                                                                                                                                                                                                                                                                |
| **N4**  | `tailwind.config.js`                          | `main` line                              | `tailwind.config.js`                                                                                                                   | `main` re-introduces the legacy `'gray-900': '#203864'` navy alias the baseline deliberately removed; lowers `accent.on-dark` from `#2dd4bf` (teal-400, documented AA 6.9:1) to `#5eead4` (teal-300); lowers `success.strong` from `#047857` (emerald-700, AA-safe 5.5:1 on white) to `#059669`; drops `'Inter Fallback'` from the font stack. Three contrast/a11y regressions.                                                                                                                                     | **YES** — the `text: { heading: '#083358' }` group is needed by A2/A8. Add **only** that group (or rewrite the imported components to the baseline's `text-heading`).                                                                                             |
| **N5**  | `CtaSection` and its copy                     | pre-`ae58074` lineage retained on `main` | `src/components/sections/CtaSection.tsx` (new on `main`), `public/locales/*/common.json` → `cta_section.*`                             | This is the forbidden band. `main`'s `de/common.json` reads `"title": "Sie suchen nach garantierter Performance und POC-Expertise?"`. The baseline has **no `cta_section` key at all** (`git show 961f65d:public/locales/de/common.json \| grep cta_section` → empty). Violates Lock 8.                                                                                                                                                                                                                             | **YES** — it is rendered from `Footer.tsx` (N6). Note `543fe41`'s own Footer comment records the decision to keep it off the entire Epigenetics track precisely because it would create "zwei konkurrierende Abschluesse, einen davon mit dem Garantie-Anspruch". |
| **N6**  | `Footer.tsx` wholesale                        | `main` line                              | `src/components/layout/Footer.tsx` (−90/+82)                                                                                           | `main`'s Footer `import CtaSection from '../sections/CtaSection'` and renders `<CtaSection />`; it also swaps the logo asset (`polaris_white.webp` → `polarisdx_logo.webp`).                                                                                                                                                                                                                                                                                                                                        | **YES** — `543fe41` touches Footer with 11 lines that are _comment only_ (the decision record above). Nothing executable is needed from `main`'s Footer.                                                                                                          |
| **N7**  | Deal / Voucher components                     | `0598e73`, `3544eab`                     | `src/components/sections/DealPopup.tsx` (311 lines), `DealHint.tsx`                                                                    | Summer-Deal popup and hint. Violates Lock 9 (Deal/Voucher deferred). The baseline already removed the Sommer-Deal (`fix/epigenetik-welle-1@37d01e7`, "wie er live schon entfernt ist").                                                                                                                                                                                                                                                                                                                             | No                                                                                                                                                                                                                                                                |
| **N8**  | Backup source files                           | `6ef376f`                                | `src/pages/HomePage.tsx.bak-nopopup`, `src/pages/ContactPage.tsx.bak-nopopup`                                                          | Editor backups of the pre-popup pages committed into `src/`. They are compiled-adjacent dead files that shadow the real pages in greps.                                                                                                                                                                                                                                                                                                                                                                             | No                                                                                                                                                                                                                                                                |
| **N9**  | `src/lib/tracking.ts`                         | `main` line                              | `src/lib/tracking.ts` (−192/+24)                                                                                                       | `main`'s version is a bare `trackEvent()` that pushes straight into `window.dataLayer`. The **baseline's** version is the provider-neutral, consent-gated interface (`setTrackingProvider`, `setTrackingConsent`, silent until both are set, no buffering — "ein Puffer waere bereits eine Vorratsdatenhaltung"). Importing `main`'s file destroys exactly the architecture Lock 10 requires.                                                                                                                       | **YES** — A2, A3, A20 all import `trackEvent`. Adapt them to `track()` instead of adopting `trackEvent`.                                                                                                                                                          |
| **N10** | `CookieBanner.tsx`                            | `main` line                              | `src/components/ui/CookieBanner.tsx` (−73/+19)                                                                                         | The baseline's banner is 73 lines more developed. Taking `main`'s is a straight regression of the consent surface.                                                                                                                                                                                                                                                                                                                                                                                                  | No                                                                                                                                                                                                                                                                |
| **N11** | `Header.tsx` wholesale                        | `main` line                              | `src/components/layout/Header.tsx` (−38/+62)                                                                                           | `main` removes the WCAG 2.5.5 `min-h-[44px]` logo hit area and the `h-11 w-11` search/menu targets (→ `p-2.5`, `h-10 w-10`), and replaces translated aria-labels with hardcoded English (`aria-label="Search"`, `aria-label="Toggle navigation"`, `alt="PolarisDX — POC-Diagnostik für Arztpraxen"`). Violates Locks 11 and the 10-language goal.                                                                                                                                                                   | **YES** — A13 (`kontaktZiel`) is in this file. Take the hunk only.                                                                                                                                                                                                |
| **N12** | Removal of `GermanOnlyPage`                   | `main` line                              | `src/App.tsx`                                                                                                                          | `main` deletes the client-side guard that forces `/vitamin-d3-implantologie` and `/s3_leitlinie` onto `/de` during in-app navigation, and adds both to the 10-language `SITEMAP_ROUTES`. Result: hand-written German copy served under `/en/`, `/fr/` … with a foreign `lang` attribute and ten near-duplicate URLs for Google.                                                                                                                                                                                     | **YES** — A4 and A9 are hunks in this same file.                                                                                                                                                                                                                  |
| **N13** | `MusterbefundPage` losses                     | `d0fdf29`                                | `src/pages/MusterbefundPage.tsx`                                                                                                       | Alongside the wanted prop refactor, `main` drops `import LanguageFallbackNotice from '../components/ui/LanguageFallbackNotice'` and the `ArrowUp` / `befund.toTop` control. `LanguageFallbackNotice.tsx` does not exist on `main` at all. Both are baseline i18n/a11y affordances.                                                                                                                                                                                                                                  | **YES** — tightly coupled to A7. Reproduce the refactor by hand.                                                                                                                                                                                                  |
| **N14** | `structuredData.ts` wholesale                 | `main` line                              | `src/components/seo/structuredData.ts` (−140/+94)                                                                                      | `main` lacks the baseline's `MONTH_NUMBERS` table and ISO-8601 date normalisation used by `createArticleSchema` for the `"28 Nov 2025"`-style dates in `src/data/articles.ts`.                                                                                                                                                                                                                                                                                                                                      | **YES** — A11 (`createItemListSchema`) is additive inside this file; A15 also touches it. Add the function, keep the baseline body.                                                                                                                               |
| **N15** | `src/data/products.ts`                        | `main` line                              | `src/data/products.ts`                                                                                                                 | Shop catalogue with hard prices (`price: 479`, `badge: 'New'`, …). Nothing imports it on `main` except `src/pages/README.de.md`. Violates Lock 9 (Shop deferred) and is dead code besides.                                                                                                                                                                                                                                                                                                                          | No                                                                                                                                                                                                                                                                |
| **N16** | `scripts/convert-og-image.js`                 | `main` line                              | `scripts/convert-og-image.js`                                                                                                          | A `.js` duplicate of the baseline's `scripts/convert-og-image.mjs`; importing it produces two scripts with the same job.                                                                                                                                                                                                                                                                                                                                                                                            | No                                                                                                                                                                                                                                                                |
| **N17** | Any wholesale page/section import from `main` | `main` line                              | the 17 Sales-Machine section files + `LegalLayout.tsx`, `ImagePlaceholder.tsx`, `LanguageFallbackNotice.tsx`, `src/lib/articleMeta.ts` | These exist on the baseline and **not** on `main` (`git diff --name-status 961f65d d0fdf29 \| awk '$1=="D"'`). Any file-level or tree-level import from `main` deletes them, i.e. deletes the Sales-Machine art direction. Violates Lock 1.                                                                                                                                                                                                                                                                         | **YES** — this is why every §4 candidate that touches an existing file is REIMPLEMENT rather than IMPORT.                                                                                                                                                         |
| **N18** | `main`'s script set                           | `main` line                              | `scripts/check-color-tokens.mjs`, `check-i18n-home.mjs`, `check-meta-descriptions.mjs`, `i18n/sync-home-from-de.mjs`                   | All four exist on the baseline and not on `main`. `check-color-tokens.mjs` is wired into `lefthook.yml` pre-commit and `package.json` `check:colors`; losing it silently disables a quality gate.                                                                                                                                                                                                                                                                                                                   | No                                                                                                                                                                                                                                                                |
| **N19** | `docs/` deletions                             | `main` line                              | `docs/deploy-preview.md`, `docs/design-system.md`, `docs/backlog.md`, `docs/global-fixes.md`, `docs/migration-map.md`                  | Present on the baseline, absent on `main`. `deploy-preview.md` is the only accurate preview runbook in the repository.                                                                                                                                                                                                                                                                                                                                                                                              | No                                                                                                                                                                                                                                                                |

---

## 6. Epigenetics Import Blueprint

Ordered, dependency-correct. **No implementation is performed here.** Each step names its evidence and its precondition.

### 6.0 Preconditions (must hold before step 1)

| Precondition                                                              | Evidence that it already holds                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `epigenetics` locale namespace exists in all 10 languages on the baseline | `git ls-tree 961f65d public/locales/de/` → `epigenetics.json` present; same for the other nine                                                                                                                                                                                                                                |
| Every key `EpiSubpage` reads is present on the baseline                   | Verified individually: `principle.caption`, `principle.title`, `evidence.caption`, `evidence.title`, `downloads.caption`, `downloads.title`, `breadcrumb.home`, `breadcrumb.current`, `befund.navChapters`, `befund.navBack`, `hero.ctaQuote`, `basics.title` → all `present` on `961f65d:public/locales/de/epigenetics.json` |
| `common:read_more` exists                                                 | `961f65d:public/locales/de/common.json:66` → `"read_more": "Weiterlesen"`                                                                                                                                                                                                                                                     |
| All nine component/lib dependencies of `EpiSubpage` exist on the baseline | `ChapterNav.tsx`, `Breadcrumbs.tsx` (with `variant: 'light' \| 'dark'` at line 11), `Reveal.tsx`, `PageTransition.tsx`, `seo/index.ts` (exports `createBreadcrumbSchema`), `SEOHead.tsx`, `lib/tracking.ts`, `lib/translationStatus.ts`, `lib/useScrollDepth.ts` — all `YES` on `961f65d`                                     |
| Tailwind utilities used by `EpiSubpage` exist                             | `max-w-container`, `accent-on-dark`, `shadow-card`, `brand-primary`, `brand-deep` all in `961f65d:tailwind.config.js` — **except `text-text-heading`**, see step 1                                                                                                                                                            |

### 6.1 Ordered blueprint

**Step 1 — Token prerequisites (AP05).**
Resolve the one token gap before any component lands. Either add the `text: { heading: '#083358' }` group to the baseline's `tailwind.config.js` (it resolves to the identical hex as the baseline's existing `heading`), **or** rewrite `text-text-heading` → `text-heading` in the two files that use it (`EpiSubpage.tsx`, and `ChapterNav.tsx`'s switcher `<summary>`). Do **not** take the rest of `main`'s `tailwind.config.js` (§5, N4).
_Evidence:_ `git diff 961f65d d0fdf29 -- tailwind.config.js`.

**Step 2 — Tracking adapter decision (AP23 gate for AP15).**
`EpiSubpage`, the three depth pages and `MusterbefundPage` all call `trackEvent(name, params)`. The baseline offers `track(ereignis: TrackingEreignis)` with a closed union of four event shapes (`AufklappEreignis`, `ScrolltiefeEreignis`, `PanelwahlEreignis`, `AnfrageEreignis`). Decide once: extend the baseline union with the depth-page enquiry event, or add a thin `trackEvent`-shaped shim over `track()`. **Never** replace the baseline module (§5, N9).
_Evidence:_ `git show 961f65d:src/lib/tracking.ts | grep ^export` vs `git show d0fdf29:src/lib/tracking.ts | grep ^export`.

**Step 3 — Foundations: `epigenetics/tokens.ts` (A1, IMPORT).**
Copy the file verbatim. No imports, no adaptation. It must land before `EpiSubpage`, which imports `LABEL`, `metaDescription` and `VERTIEFUNGEN` from it.

**Step 4 — `ChapterNav` hunks (A8, IMPORT).**
Add `onClick?: () => void` to `NavAction` (hard dependency of `EpiSubpage`'s action slot), `data-chapterbar=""` on the sticky wrapper, and the `sr-only` switcher label. Take these three hunks only.

**Step 5 — `EpiSubpage` (A2, REIMPLEMENT).**
Recreate against the baseline with the step-1 token and step-2 tracking decisions applied. Preserve the two contracts documented in its header:

- the enquiry link stays `?intent=quote&source=epigenetics` — a value like `epigenetics-grundlagen` would silently disable the form's Epigenetics mode, because `ContactForm.tsx` compares `params.get('source') === 'epigenetics'` exactly;
- the page identity goes into the _event_, not the URL (`source` prop: `grundlagen` / `studienlage` / `unterlagen`).

**Step 6 — Foundations page: `/epigenetics/grundlagen` (A3).**
`EpigeneticsBasicsPage.tsx`, built from existing `principle.*` / `basics.*` keys.

**Step 7 — Studies page: `/epigenetics/studienlage` (A3).**
`EpigeneticsEvidencePage.tsx`, built from existing `evidence.*` keys.

**Step 8 — Resources page: `/epigenetics/unterlagen` (A3).**
`EpigeneticsDocsPage.tsx`, built from existing `downloads.*` keys and `ASSET_BASE` from step 3.
_Known content constraint carried over from `projektverzeichnis/10-befunde.md` §8:_ `public/downloads/epigenetics/de/` holds 17 PDFs, `en/` only 9. The resources page will therefore be asymmetric across languages until the missing English PDFs exist. This is a content gap, not an import problem.

**Step 9 — Routes for steps 6–8 (A4, REIMPLEMENT).**
Three `lazy()` imports plus three `<Route>` blocks in the baseline's `src/App.tsx`. Nothing else from `main`'s App.tsx except step 12.

**Step 10 — Sitemap + 404 table (A10, REIMPLEMENT).**
Three `SITEMAP_ROUTES` entries in the baseline's `server.ts`. Because the baseline builds `KNOWN_PATHS` from `SITEMAP_ROUTES`, this is also what stops the three new URLs answering HTTP 404. **This is the single most important reason `server.ts` must be edited, never replaced.**

**Step 11 — Musterbefund route modules (A6 → A7 → A5).**

1. Add `src/content/befunde/meta.ts` (A6, IMPORT).
2. Re-point `src/content/befunde/index.ts` at it and refactor `MusterbefundPage` to accept `befunde` + `slug` as props (A7, REIMPLEMENT) — **keeping** the baseline's `LanguageFallbackNotice` and `befund.toTop` control (§5, N13).
3. Add the six 20-line route modules (A5, IMPORT).
4. Add the six explicit routes plus the `:slug` catch-all **after** them in `App.tsx` — the ordering comment on `main` is load-bearing: _"Muss NACH den sechs stehen: faengt unbekannte Slugs ab."_
   The six sitemap entries for these URLs are **already on the baseline** — they appear as context lines in `git diff 961f65d d0fdf29 -- server.ts`. No sitemap work is needed here.

**Step 12 — Anchor-jump guard (A9, IMPORT).**
The `ScrollToHash` `[data-chapterbar]`-ready check. Depends on step 4(b). Fixes direct-entry anchors on every chapter-bar page, including `/de/epigenetics#vergleich`, which is printed in the delivered PDFs.

**Step 13 — Funnel closure (A12 → A13 → A14).**
Dual origin contract in `ContactForm`, then the header contact target, then the mail-subject origin. In that order: A13 emits `?topic=epigenetik`, which A12 must already understand, and A14 formats what both produce.

**Step 14 — Metadata/SEO polish (A11, A16).**
`createItemListSchema` + barrel export; `bigResult` in `ALWAYS_OPEN`; the `ZAEHLBAR` value counter.

**Step 15 — Deferred to AP23.** A19 (CSP), A20 (instrumentation), A18 (chat removal).

### 6.2 Assets NOT part of this blueprint

`main`'s `EpigeneticsPage.tsx` (949 lines of churn against the baseline's version, which the locked baseline owns), `Footer.tsx`, `Header.tsx` beyond A13, `CookieBanner.tsx`, `index.html`, `tailwind.config.js` beyond step 1, `SEOHead.tsx`, `lib/tracking.ts`, `server.ts` beyond step 10.

---

## 7. redesign/preview@5673b61 — Neutral Engineering Patterns

`redesign/preview` is **not a design source.** It is 31 commits past `ae58074`, dated 2026-06-26, and adds 127 files. Only the art-direction-neutral engineering subset is considered below.

| Candidate                                | Commit(s)                       | Files                                                                                                                              | Status             | Adaptation Required                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | AP                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------- | ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| **B1** Error boundaries                  | `a44ecc5` (Phase 5 §5.9)        | `src/routing/ErrorBoundary.tsx`, `RootErrorBoundary.tsx`, `SegmentErrorBoundary.tsx`, `RouteFallback.tsx`, `index.ts`              | **REIMPLEMENT**    | **Yes.** Imports are clean — only `react`, `react-i18next` and `../lib/monitoring` (verified per file). But: (a) they need locale keys `errors.root.title`, `errors.root.retry`, `errors.root.home`, `errors.segment.title`, `errors.segment.retry`, `a11y.loading` — **none exist on the baseline** (`961f65d:public/locales/de/common.json` has only a generic `"error"` string at line 90) — so 6 keys × 10 languages; (b) `ErrorBoundary` calls `reportError` from B2, which must land first or be stubbed.                                                                                                                              | AP10 / AP24                                                                                                                                                                                                                                                                                                                                                |
| **B2** Web Vitals + error reporting sink | `a44ecc5` (Phase 5 §5.10)       | `src/lib/monitoring/report.ts`, `web-vitals.ts`, `index.ts`                                                                        | **REIMPLEMENT**    | **Yes — mandatory.** `report.ts` beacons to `/api/monitoring/client-error` and `/api/monitoring/web-vitals`. **Neither endpoint exists** — on `5673b61`, on the baseline, or on `main` (`git show 5673b61:server.ts \| grep -n "vitals\|metrics"` → no match; the paths merely fall into the generic `/api/` proxy at `server.ts:381`). Importing as-is fires beacons at a proxy target that will 404. Decide the sink first, or land the collector with a no-op transport. The collector itself is genuinely good: zero new dependencies, native `PerformanceObserver`, LCP/CLS/INP/FCP/TTFB with web.dev thresholds, swallows every error. | AP25                                                                                                                                                                                                                                                                                                                                                       |
| **B3** Accessibility audit gate          | `707dddf`                       | `scripts/a11y-audit.mjs`, `package.json` script `"audit:a11y": "node scripts/a11y-audit.mjs"`                                      | **IMPORT**         | **Yes, two changes.** (a) **`axe-core` is not a declared devDependency on `5673b61`** — `git show 5673b61:package.json \| grep axe` returns nothing, while the script does `require.resolve('axe-core/axe.min.js')`; it currently works only via a transitive copy in the lockfile. Add it explicitly. (b) Its route list (`/de/`, `/de/diagnostics`, `/de/articles`, `/de/contact`, `/de/imprint`, `/de/this-route-does-not-exist`) predates `/epigenetics` and the six Musterbefunde — extend it. Otherwise portable: axe WCAG 2.0/2.1/2.2 A+AA against a running SSR server, plus a horizontal-overflow assertion at 375 px and 1280 px.  | AP24 / AP27                                                                                                                                                                                                                                                                                                                                                |
| **B4** Baseline screenshot tooling       | `db88eb4`                       | `scripts/baseline-screenshots.mjs`                                                                                                 | **IMPORT**         | **Yes, one change.** Same stale route list. Otherwise dependency-clean (`playwright` only, already a devDependency) — sm/md/lg/xl full-page captures into `docs/baseline-screenshots` plus the same overflow assertion. Note the repository memory `sandbox-runtime-gates-blocked`: Chromium **does** launch in this environment, so this is runnable locally, not CI-only.                                                                                                                                                                                                                                                                  | AP27 / AP25                                                                                                                                                                                                                                                                                                                                                |
| **B5** `usePrefersReducedMotion`         | `a44ecc5`                       | `src/hooks/usePrefersReducedMotion.ts`                                                                                             | **IMPORT**         | **No.** 27 lines, imports only `useSyncExternalStore` from React, SSR-safe via `getServerSnapshot = () => false`. Covers WCAG 2.2.2 / 2.3.3 for JS-driven motion that the CSS-only `prefers-reduced-motion` rule cannot reach. No equivalent on the baseline (`git ls-tree 961f65d src/hooks/` → 8 hooks, none of them this).                                                                                                                                                                                                                                                                                                                | AP24                                                                                                                                                                                                                                                                                                                                                       |
| **B6** `Intl`-based locale formatting    | `a44ecc5` (Phase 5 §5.5)        | `src/lib/i18n/format.ts`                                                                                                           | **IMPORT**         | **No** (adoption is separate work). Native `Intl.DateTimeFormat`/`NumberFormat` keyed off `i18n.language`, falling back to `'de'` rather than `'en-US'`. Directly relevant: the baseline's `structuredData.ts` still carries a hand-maintained 40-entry `MONTH_NUMBERS` table for German and English only — this module is the 10-language replacement. Import the module now; migrating call sites is its own AP08 task.                                                                                                                                                                                                                    | AP08                                                                                                                                                                                                                                                                                                                                                       |
| **B7** Changelog gate CI job             | `a44ecc5`                       | `.github/workflows/ci.yml` (`changelog-gate` job)                                                                                  | **REIMPLEMENT**    | **Yes.** The job greps changed paths for `^(src/design-system/                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | tailwind\.config\.js)`. `src/design-system/` **does not exist on the baseline** — imported verbatim, the gate silently never fires. Re-scope it to whatever the relaunch declares as its token/component source of truth.                                                                                                                                  | AP27        |
| **B8** `CHANGELOG.md` format             | `a44ecc5`                       | `CHANGELOG.md`                                                                                                                     | **REFERENCE_ONLY** | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Its convention (change type **markup/style/script/spec** × group **new/enhancement/fix/other**, SemVer + date, user-visible first) is a good pattern. Its _content_ is a log of the June redesign phases and must not enter the baseline's history.                                                                                                        | AP27        |
| **B9** `CODEOWNERS`                      | `a44ecc5`                       | `.github/CODEOWNERS`                                                                                                               | **REIMPLEMENT**    | **Yes.** Every path it guards (`src/design-system/**`, `src/pages/StyleguidePage.tsx`, `e2e/styleguide-visual.spec.ts`, `docs/design-system/**`) is redesign-only. It also names a placeholder team `@design-system-owners` that its own header flags as _"ASSUMPTION — needs human confirmation"_. Re-scope and re-team, or skip.                                                                                                                                                                                                                                                                                                           | AP27                                                                                                                                                                                                                                                                                                                                                       |
| **B10** Metric-honesty definitions       | `43b04b3`                       | `src/lib/metrics/definitions.ts`, `thresholds.ts`, `aggregate.ts`, `aggregate.test.ts`                                             | **REFERENCE_ONLY** | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | A vocabulary for outcome-vs-vanity metrics with mandatory `hypothesis` / `whatItProxies` / `validityCaveat` / `scaleType` fields. Defines nothing runtime and collects nothing. Valuable as a checklist when AP23 defines the event catalogue; not source code for the relaunch. `aggregate.test.ts` is a usable example of the repository's vitest style. | AP23 / AP27 |
| **B11** UX research + persona corpus     | `43b04b3`                       | `docs/ux/*` (9 files), `docs/personas/*` (3 files), `docs/interface-inventory.md`, `docs/REFACTOR_BACKLOG.md`, `docs/GRAVEYARD.md` | **REFERENCE_ONLY** | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Research artefacts (heuristics audit, dark-patterns checklist, voice-and-tone, three personas). Useful evidence; must not become tracked relaunch documentation competing with `projektverzeichnis/`.                                                                                                                                                      | AP05        |
| **B12** Design-system component docs     | `02b716d`, `c6ce46b`, `b9d3635` | `docs/design-system/**` (36 files)                                                                                                 | **REFERENCE_ONLY** | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Documents the _alternative_ art direction's components. Read for structure (per-component spec pages, `lineage.md` Uses/Used-by), never for content.                                                                                                                                                                                                       | AP05        |
| **B13** Dependency-cycle config          | `a44ecc5`                       | `.madgerc`                                                                                                                         | **DEFER**          | **Yes.** 6 lines of madge config, but `madge` is not a dependency on any branch and no script invokes it. Only worth taking together with a real cycle-check gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | AP27                                                                                                                                                                                                                                                                                                                                                       |

---

## 8. redesign/preview — Art Direction / Legacy Exclusions

Everything below must **not** cross into the baseline. Each violates Lock 1, Lock 2, Lock 9, or Lock 11.

| #       | Item                                      | Files                                                                                                                                                                                                                        | Reason                                                                                                                                                                                                                                                                                                                                                         |
| ------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **X1**  | The entire alternative design system      | `src/design-system/**` (29 files: `core/`, `compound/`, `feedback/`, `primitives-layout/`, `tokens/tokens.ts`, `tokens/tokens.css`, `index.ts`)                                                                              | This _is_ the alternative art direction the locks reject. It carries its own token layer (`tokens.css`) that would compete with `tailwind.config.js`, and its own `button`, `input`, `card`, `panel`, `breadcrumbs`, `gradient-hero`, `gradient-surface`. Violates Lock 1 and Lock 3.                                                                          |
| **X2**  | Pattern-library page and its visual suite | `src/pages/StyleguidePage.tsx`, `e2e/styleguide-visual.spec.ts`, the `Visual regression (Pattern Library)` CI step                                                                                                           | The spec screenshots 20+ named specimens (`container`, `stack`, `cluster`, `grid`, `button`, `input`, `panel`, `accordion`, `breadcrumbs`, …) that exist only inside X1. Without X1 the suite cannot run; with X1 the locks are broken. **This is the hidden coupling to watch for** — B7's changelog gate and B9's CODEOWNERS both point at these same paths. |
| **X3**  | Redesign home composition                 | `src/components/sections/home/{HomeHero,HomeDomains,HomeApproach,HomeFocusGrid,HomeMagazine,HomeProductSpotlight,HomeTrustBar,HomeVoices,SectionIntro}.tsx`                                                                  | An alternative home page built on X1. Directly replaces the Sales-Machine home. Violates Lock 1.                                                                                                                                                                                                                                                               |
| **X4**  | Alternative CTA bands                     | `src/components/sections/CtaBand.tsx`, `src/components/sections/CtaSection.tsx`, `src/components/sections/ContactChannels.tsx`                                                                                               | `CtaSection` here is the same forbidden band as §5 N5 — `redesign/preview` also carries `cta_section.*` with "garantierte Performance" in `de/common.json` (`git grep -il garantierte 5673b61` → `public/locales/de/common.json`, `services.json`). `CtaBand` is the layout-preserving replacement variant the task brief warns about. Violates Lock 8.        |
| **X5**  | Static sitemap                            | `public/sitemap.xml`                                                                                                                                                                                                         | The baseline has **no** static sitemap by design — `server.ts` generates it from `SITEMAP_ROUTES` with hreflang alternates. Adding a static file re-creates the sitemap-shadowing class of bug that `main@f0a1b0e` had to fix. Violates Lock 11.                                                                                                               |
| **X6**  | Shop catalogue                            | `src/data/products.ts`                                                                                                                                                                                                       | Same file and same reason as §5 N15. Violates Lock 9.                                                                                                                                                                                                                                                                                                          |
| **X7**  | Feature-flag stub                         | `src/lib/flags.ts`                                                                                                                                                                                                           | Declares exactly one flag named `placeholder: false` with the comment _"durch echte Flags ersetzen"_. Dead code with no consumer.                                                                                                                                                                                                                              |
| **X8**  | Redesign imagery                          | `public/images/{clinic-consultation,doctor-igloopro,igloopro-device,igloopro-frontal}.webp`                                                                                                                                  | Assets chosen for X3's composition.                                                                                                                                                                                                                                                                                                                            |
| **X9**  | Archon orchestration + phase plans        | `.archon/config.yaml`, `.archon/workflows/polaris-{newlook-home,redesign-phase,relaunch,variants}.yaml`, `EXECUTION-PLAN.md`, `REFACTOR-LOG.md`, `docs/RELAUNCH-CONCEPT.md`, `docs/RELAUNCH-PLAN.md`, `docs/NEWLOOK-HOME.md` | Steering documents for the June redesign programme. They describe a plan the locks have superseded; importing them creates a second, contradictory relaunch plan in-repo.                                                                                                                                                                                      |
| **X10** | Duplicate og-image script                 | `scripts/convert-og-image.js`                                                                                                                                                                                                | Same `.js`-vs-`.mjs` duplication as §5 N16.                                                                                                                                                                                                                                                                                                                    |

**Hidden-coupling summary.** No dark-theme code was found on `5673b61` (consistent with the repository memory `main-site-dark-theme-split`: `surface`/`ink`/`brand.sky` live on `demo-relaunch`, not here). The real coupling risks are: (a) B1 → B2 → a non-existent `/api/monitoring/*` sink; (b) B7 and B9 → `src/design-system/**` (X1); (c) B3 → an undeclared `axe-core`. The branch does **not** carry an alternate Header/Footer that would sneak in — its `Header.tsx`/`Footer.tsx` changes are token refactors inside X1's scheme and are excluded with it.

---

## 9. feat/contact-joyful@ab373a3

**Status: ALREADY_PRESENT (implementation) / REFERENCE_ONLY (branch).**

The branch is a single commit — `feat(contact): joyful single-page contact form with live progress meter`, 2026-07-03, one commit past the shared ancestor `ae58074` (`git log -1 ab373a3^` → `ae58074`). It touches 6 files: `public/locales/{de,en}/contact.json`, `src/api/contact.ts`, `src/components/sections/ContactForm.tsx`, `src/hooks/useContactForm.ts`, `src/pages/ContactPage.tsx`.

**Its implementation is already on the locked baseline, and the baseline has gone further.**

| Pattern claimed by `ab373a3` | On the baseline?              | Evidence                                                                                                                                                                        |
| ---------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Live completion meter (x/5)  | **Yes**                       | `961f65d:src/components/sections/ContactForm.tsx:278` → `role="progressbar"`; `:135` → the same `progressMessage` ladder over `contact.form.progress.{start,done,almost,going}` |
| "Five joyful steps" logic    | **Yes — same source comment** | baseline `:127` `// Five joyful"steps" that fill the progress meter.` vs `ab373a3` `:42` `// Five joyful "steps" that fill the progress meter.`                                 |
| Intent-adaptive submit label | **Yes**                       | `961f65d:public/locales/de/contact.json` → `submit: { consultation, quote, product, support, other }`, with `"quote": "Angebot anfragen"` — already the value Lock 7 requires   |
| Intent / field pills         | **Yes**                       | same `pillClass` / `aria-pressed` construction present on both                                                                                                                  |
| Inline valid checkmarks      | **Yes**                       | `Check` icon usage present on both                                                                                                                                              |
| EN/DE i18n                   | Baseline is broader           | baseline ships `contact.json` in all 10 languages                                                                                                                               |

**And the baseline adds what `ab373a3` lacks:**

- programmatic field errors — `aria-invalid` / `aria-describedby` on the field and consent inputs (`:380`, `:381`, `:468`, `:469`);
- a live region — `role="alert"` (`:445`);
- focus management on submit outcome — `if (submitStatus === 'success') successRef.current?.focus()` (`:166–167`, `:192`);
- Epigenetics panel context — `sourceParam` / `showPanelContext` (`:74`, `:85`), which `ab373a3` has no concept of.

**Quantitatively:** `git diff --stat 961f65d ab373a3` over the four source files reports **105 insertions against 514 deletions** — moving to `ab373a3` would strip 514 lines and add 105. It is a strict subset.

`git cherry 961f65d ab373a3` prints `+ ab373a3…` (no patch-equivalent commit), but that reflects the divergent lineage, not missing functionality — the functionality arrived on the baseline through its own line (`bdbd042` Panel-Hinweis/Live-Regionen/Feldfehler/Fokusfuehrung, `9167a19` Panelparameter, `8842a1f` Absendeknopf "Angebot anfragen").

**Recommendation:** import nothing. Keep the branch as design evidence only. Against the stated final target — contact becoming a persistent CRM-backed lead journey under Sales-Machine art direction — its single-page, client-state-only form is not a foundation to build on. **Do not recommend it on polish.**

---

## 10. Cross-Branch Dependency Graph

Human-readable dependency order. `→` means "must land before".

```
LAYER 0 — decisions that unblock everything else
  D1  Token gap: add tailwind `text.heading`  OR  rewrite `text-text-heading` → `text-heading`
  D2  Tracking adapter: extend baseline `track()` union  OR  add a `trackEvent` shim over it
  D3  Monitoring sink: define /api/monitoring/*  OR  land B2 with a no-op transport
  D1 → A2, A3, A8(c)
  D2 → A2, A3, A20
  D3 → B2 → B1

LAYER 1 — dependency-free imports (any order)
  A1  epigenetics/tokens.ts
  A6  content/befunde/meta.ts
  A11 createItemListSchema + seo/index.ts export
  B5  usePrefersReducedMotion
  B6  lib/i18n/format.ts
  B4  scripts/baseline-screenshots.mjs        [adapt route list]
  B3  scripts/a11y-audit.mjs                  [adapt route list + declare axe-core]

LAYER 2 — component prerequisites
  A8  ChapterNav: NavAction.onClick, data-chapterbar, sr-only switcher label
      A8(a) → A2          (EpiSubpage's action slot needs onClick)
      A8(b) → A9          (ScrollToHash guard queries [data-chapterbar])
  A7  MusterbefundPage prop refactor + befunde/index.ts split
      A6 → A7 → A5

LAYER 3 — Epigenetics surface
  A1 + A8(a) + D1 + D2 → A2  EpiSubpage
  A2 → A3                     Basics / Evidence / Docs pages
  A3 → A4                     three routes in App.tsx
  A4 → A10                    three SITEMAP_ROUTES lines  (also feeds KNOWN_PATHS → real 404)
  A5 → A4'                    six explicit musterbefund routes, catch-all LAST
  A8(b) → A9                  anchor-jump guard

LAYER 4 — funnel closure (strict order)
  A12  ContactForm dual origin contract
   → A13  Header kontaktZiel emits ?topic=epigenetik
   → A14  server/server.js mail-subject origin

LAYER 5 — polish
  A16  ALWAYS_OPEN bigResult + ZAEHLBAR value counter      [needs A7]
  A15  phone-number unification                            [independent]

LAYER 6 — quality gates
  B2 → B1  (error boundaries need reportError)
  B1 → 6 locale keys × 10 languages (errors.root.*, errors.segment.*, a11y.loading)
  B7  changelog gate      [re-scope paths first — src/design-system/ does not exist]
  B9  CODEOWNERS          [re-scope paths + real team]
  B13 .madgerc            [only with a real cycle-check gate]

LAYER 7 — AP23, after the consent architecture is settled
  A18  chat removal recipe  →  A19  CSP domain removal  →  A20  event instrumentation
```

**Conflict edges (things that must never be satisfied by importing a whole file):**

```
A4, A9    live in src/App.tsx        ✗ conflicts with N12 (GermanOnlyPage removal)
A10       lives in server.ts         ✗ conflicts with N1  (404/redirect/cache hardening)
A11, A15  live in structuredData.ts  ✗ conflicts with N14 (date normalisation)
A13       lives in Header.tsx        ✗ conflicts with N11 (44px targets, i18n aria)
A7, A16   live in MusterbefundPage   ✗ conflicts with N13 (LanguageFallbackNotice, toTop)
A2, A3    import trackEvent          ✗ conflicts with N9  (consent-gated track())
A2, A8(c) use text-text-heading      ✗ conflicts with N4  (legacy gray-900, contrast drops)
B7, B9    reference src/design-system ✗ conflicts with X1  (alternative art direction)
B1        calls reportError          ✗ blocked by  B2/D3  (no /api/monitoring/* endpoint)
```

---

## 11. High-Risk Shared Files

| File                                      | Baseline Role                                                                                                                                                       | Source Changes                                                                                                                                                             | Risk         | Handling                                                                                                                                                                                                                                  |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/App.tsx`                             | Route table for the whole site; `ScrollToHash`; `GermanOnlyPage` guard; `MainLayout` incl. `ChatWidget`                                                             | `main`: +9 routes, +6 lazy imports, `ScrollToHash` chapterbar guard **(wanted)**; **removes `GermanOnlyPage`**, removes `ChatWidget`                                       | **CRITICAL** | Hunk-level only. Take the route blocks (A4, A5) and the `ScrollToHash` guard (A9). **Never** take the `GermanOnlyPage` deletion. The `ChatWidget` deletion is correct in direction but belongs to AP23 (A18).                             |
| `server.ts`                               | Real HTTP 404 (`isKnownPath` + `NOT_FOUND_MARKER`), legacy 301s, German-only 301s, dynamic sitemap + hreflang, CSP, `no-store` HTML, font preload, `127.0.0.1` bind | `main`: **+3 sitemap routes (wanted)**; removes every item listed in the baseline role column                                                                              | **CRITICAL** | Add three `SITEMAP_ROUTES` lines by hand (A10). Later, delete two HiHuman CSP tokens (A19). Nothing else, ever.                                                                                                                           |
| `src/components/layout/Header.tsx`        | Mega menu, WCAG 2.5.5 44 px hit areas, translated aria-labels                                                                                                       | `main`: `kontaktZiel` for the Epigenetics track **(wanted)**; shrinks hit areas to `p-2.5` / `h-10 w-10`, hardcodes English aria-labels, changes CTA variant/shadow        | **HIGH**     | Take the `kontaktZiel` hunk (A13) only.                                                                                                                                                                                                   |
| `src/components/layout/Footer.tsx`        | Footer without the guarantee CTA band                                                                                                                               | `main`: renders `<CtaSection />` (the "garantierte Performance" band), swaps the logo asset; `543fe41` adds an 11-line **comment-only** decision record                    | **HIGH**     | Do not import. Optionally transcribe the `543fe41` comment as rationale.                                                                                                                                                                  |
| `src/components/seo/SEOHead.tsx`          | `notFound` prop → soft-404 marker meta; `GERMAN_ONLY_PATHS` canonical/hreflang handling                                                                             | `main`: −63/+21, deletes both                                                                                                                                              | **HIGH**     | Do not import. No wanted change identified in this file.                                                                                                                                                                                  |
| `src/i18n.ts`                             | Namespace registration and fallback chain                                                                                                                           | `git diff 961f65d d0fdf29 -- src/i18n.ts` → **empty**                                                                                                                      | **LOW**      | Identical on both refs. No action. Note the standing issue from `projektverzeichnis/10-befunde.md` §3: `casestudies.json` ships in all 10 languages but is registered in no `NAMESPACES` entry — a baseline matter, not an import matter. |
| `src/pages/EpigeneticsPage.tsx`           | The programme page; owned by the locked baseline                                                                                                                    | `main`: 949 lines of churn (436/513) — the three-page split removed chapters that the baseline still carries                                                               | **CRITICAL** | Do not import. The split (A2–A4) changes this page as a **consequence**; that edit is authored against the baseline, not copied from `main`. Also: the uncommitted working-tree change in this file (§3) is out of scope and untouched.   |
| `tailwind.config.js`                      | Token source; `heading` navy; AA-verified `accent.on-dark` `#2dd4bf`; `success.strong` `#047857`; `'Inter Fallback'`                                                | `main`: adds `text.heading` **(wanted)**; re-adds `gray-900` navy alias, lowers `accent.on-dark` → `#5eead4`, lowers `success.strong` → `#059669`, drops the font fallback | **HIGH**     | Add the `text: { heading: '#083358' }` group only — or avoid it entirely by renaming the utility in the two imported components.                                                                                                          |
| `index.html`                              | No global `meta keywords`; robots/googlebot as documented Helmet fallback; GTM + Consent Mode v2 default-denied                                                     | `main`: re-adds a static German `meta keywords`, removes the robots-fallback contract comment. GTM/consent block is **byte-identical** on both                             | **HIGH**     | Do not import. The consent scaffolding (`gtag('consent','default', …)` before GTM, `localStorage` `cookie-consent` read) needs no change from any source branch.                                                                          |
| `package.json`                            | Node-22-targeted toolchain; `check:colors` gate; lefthook `prepare`                                                                                                 | `redesign/preview`: adds `"audit:a11y"` **(wanted)**, and devDeps `@testing-library/*`, `eslint-plugin-boundaries`, `jsdom`                                                | **MEDIUM**   | Add the `audit:a11y` script plus an explicit `axe-core` devDependency (B3). Evaluate other devDeps individually; `eslint-plugin-boundaries` is an X1 artefact.                                                                            |
| `vite.config.ts`                          | SSR externals/noExternal, `allowedHosts: ['relaunch.polarisdx.net']`, dev proxy → `:5000`                                                                           | `git diff 961f65d d0fdf29 -- vite.config.ts` → **no material change**                                                                                                      | **LOW**      | No action. Note that chunk splitting for the Musterbefunde is achieved by module structure (A5), not by config.                                                                                                                           |
| `server/server.js`                        | Mail service; `formLimiter` on 3 of 4 form routes; `/api/chat` mock; ROI PDF endpoint                                                                               | `main`: origin-in-subject hunk **(wanted, +45)**; overall −187/+43 vs the baseline                                                                                         | **MEDIUM**   | Take the origin/subject hunk (A14) only. Independently, `POST /api/consumer-order` remains unrate-limited on both refs (`projektverzeichnis/10-befunde.md` §6) — an AP10/AP23 backlog item, not an import.                                |
| `src/lib/tracking.ts`                     | Provider-neutral, consent-gated, silent-by-default event interface                                                                                                  | `main`: replaced by a direct `dataLayer` push (−192/+24)                                                                                                                   | **CRITICAL** | Do not import. It is the single file whose `main` version most directly contradicts Lock 10.                                                                                                                                              |
| `src/components/sections/ContactForm.tsx` | 10-language form with progress meter, live region, field errors, focus restoration, Epigenetics panel context                                                       | `main`: dual origin contract **(wanted)**; 646 lines of other churn                                                                                                        | **HIGH**     | Take the origin condition (A12) only.                                                                                                                                                                                                     |
| `src/pages/MusterbefundPage.tsx`          | Six-panel report renderer, chapter bar, language-fallback notice, back-to-top                                                                                       | `main`: prop-based chunk split **(wanted)**, `bigResult`/`ZAEHLBAR` **(wanted)**; drops `LanguageFallbackNotice` and `befund.toTop`                                        | **HIGH**     | Reimplement the three wanted behaviours; keep both baseline affordances.                                                                                                                                                                  |

---

## 12. Commit Safety Matrix

Conservative by construction: because the branches diverged 312 files ago and `main` is older on all shared infrastructure, **no commit from any source branch qualifies as `SAFE_TO_CHERRY_PICK`.**

| Commit                                                                                                                                                   | Purpose                                                                | Classification         | Reason                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `b699435`                                                                                                                                                | Epigenetik-Strecke: Vertiefung auf drei Unterseiten                    | **PARTIAL_ONLY**       | Adds `EpiSubpage.tsx`, `tokens.ts` and the three depth pages (all wanted) but in the same commit rewrites `src/pages/EpigeneticsPage.tsx` by 689 lines, edits `EpigeneticsPanels.tsx`, `EpigeneticsTeaserSection.tsx`, `MusterbefundPage.tsx`, `server.ts` and `src/App.tsx`. Take `tokens.ts` verbatim; reimplement the rest.                                                                        |
| `543fe41`                                                                                                                                                | AP1 — die drei Vertiefungsseiten fertig                                | **PARTIAL_ONLY**       | Wanted: the `ChapterNav` `onClick`/`data-chapterbar` hunks (+19) and the finished `EpiSubpage` (+119). Unwanted-by-coupling: `src/components/layout/Footer.tsx` (+11) — comment-only, so harmless to skip — and further `App.tsx`/`EpigeneticsPage.tsx` edits.                                                                                                                                        |
| `05a58f2`                                                                                                                                                | AP2 — den Trichter schliessen                                          | **PARTIAL_ONLY**       | Wanted: `ContactForm` dual origin (+24), `Header` `kontaktZiel` (+23), `server/server.js` subject/origin (+45). Unwanted: 108 lines in `ContactPage.tsx` and 37 in `MusterbefundPage.tsx` against a baseline that has diverged in both.                                                                                                                                                               |
| `86fd38d`                                                                                                                                                | AP3 — die erste Entscheidung nach vorn                                 | **PARTIAL_ONLY**       | Wanted: `createItemListSchema` (+40 in `structuredData.ts`, +1 in `seo/index.ts`). Unwanted: 212 lines in `EpigeneticsPage.tsx`, 23 in `EpigeneticsPanels.tsx`.                                                                                                                                                                                                                                       |
| `d0fdf29`                                                                                                                                                | AP4 — die laengste Seite der Site                                      | **PARTIAL_ONLY**       | Wanted: `content/befunde/meta.ts` (new), the six 20-line route modules (new), the six routes in `App.tsx`, `bigResult`/`ZAEHLBAR`. Unwanted: the `LanguageFallbackNotice` and `befund.toTop` removals inside the same `MusterbefundPage.tsx` hunk set (+181/−?).                                                                                                                                      |
| `475fefd`                                                                                                                                                | Rufnummern vereinheitlicht                                             | **DO_NOT_CHERRY_PICK** | Touches 23 files, 12 of which (`src/content/befunde/*.json`) have diverged between the branches, and 10 more (`public/locales/*/vitd3spray.json`) sit in a namespace with 319 lines of churn per language. The baseline also carries **three** distinct numbers where `main` carries one — a straight cherry-pick would unify only the subset `main` happens to touch. Re-apply as a data pass (A15). |
| `c59cf5d`                                                                                                                                                | Alle vier Anfragewege messbar                                          | **DO_NOT_CHERRY_PICK** | Both files it touches (`BefundBlocks.tsx`, `EpigeneticsPage.tsx`) depend on `main`'s `trackEvent`, i.e. on the `lib/tracking.ts` the baseline must not accept. Defer to AP23 (A20).                                                                                                                                                                                                                   |
| `e21a6e5`                                                                                                                                                | Tabellenkopfzeile bleibt beim Scrollen stehen                          | **DO_NOT_CHERRY_PICK** | Its target file `src/pages/EpigeneticsPage.tsx` is **currently dirty in this working tree** with a partial replay of this very commit (§3), and the baseline's version of the wrapper differs structurally (`hidden … lg:block` on `main` vs no breakpoint gate on the baseline). Out of scope for this task; flagged so no later agent tries it blind.                                               |
| `0598e73`, `3544eab`                                                                                                                                     | Sommer-Deal Popup / Hinweis                                            | **DO_NOT_CHERRY_PICK** | Violates Lock 9. Already deliberately removed on the baseline (`37d01e7`).                                                                                                                                                                                                                                                                                                                            |
| `6ef376f`                                                                                                                                                | Neuer erster Bildschirm auf allen sechs Panel-Seiten                   | **DO_NOT_CHERRY_PICK** | Introduces the two `.bak-nopopup` files (§5 N8); the baseline has its own equivalent work (`dff3981`, `d57bab6`).                                                                                                                                                                                                                                                                                     |
| `02b716d`, `42e72fd`, `4a0c947`, `2de19b8`, `40e9205`, `3aacc4e`, `27f0124`, `56a9356`, `2d724df`, `e7ac660`, `746ff4e`, `8a5477a`, `5852a05`, `bffb7de` | redesign/preview Phase 1–4 token & typography passes                   | **DO_NOT_CHERRY_PICK** | These are the alternative art direction (X1). Violates Locks 1–3.                                                                                                                                                                                                                                                                                                                                     |
| `a44ecc5`                                                                                                                                                | Phases 5–7 — A11y/Humanity/Sustainability, UX-Validierung & Governance | **PARTIAL_ONLY**       | The single commit carrying B1, B2, B5, B6, B7, B9, B13 **and** large parts of X1. It cannot be cherry-picked; extract the named files individually.                                                                                                                                                                                                                                                   |
| `707dddf`                                                                                                                                                | a11y-Audit-Gate (Playwright+axe)                                       | **PARTIAL_ONLY**       | `scripts/a11y-audit.mjs` is portable but needs a declared `axe-core` and a current route list; the commit also carries redesign-era documentation.                                                                                                                                                                                                                                                    |
| `db88eb4`                                                                                                                                                | Capture 20 baseline screenshots                                        | **PARTIAL_ONLY**       | Take `scripts/baseline-screenshots.mjs`; leave the 20 captured PNGs of the June design.                                                                                                                                                                                                                                                                                                               |
| `43b04b3`                                                                                                                                                | Close audit/baseline — inventory, personas, backlog, metrics           | **PARTIAL_ONLY**       | Documentation and `src/lib/metrics/*` — REFERENCE_ONLY material only.                                                                                                                                                                                                                                                                                                                                 |
| `889d57e`, `8a7b938`, `5673b61`                                                                                                                          | "preview snapshot (incl. uncommitted WIP)"                             | **DO_NOT_CHERRY_PICK** | Self-declared dirty-tree snapshots. Committing a whole working tree wholesale makes the contents unattributable to any intent. `5673b61` is the branch tip and is only usable as a **read** surface.                                                                                                                                                                                                  |
| `ab373a3`                                                                                                                                                | joyful single-page contact form                                        | **DO_NOT_CHERRY_PICK** | Functionality already present on the baseline in a strictly broader form; the diff would remove 514 lines to add 105 (§9).                                                                                                                                                                                                                                                                            |

---

## 13. AP Mapping

| Candidate                         | AP          | Earliest Safe Import Point | Preconditions                                                                |
| --------------------------------- | ----------- | -------------------------- | ---------------------------------------------------------------------------- |
| A1 `epigenetics/tokens.ts`        | AP15        | AP01                       | none                                                                         |
| A2 `EpiSubpage`                   | AP15        | AP15                       | D1 (token), D2 (tracking), A1, A8(a)                                         |
| A3 three depth pages              | AP15        | AP15                       | A2                                                                           |
| A4 depth-page routes              | AP10        | AP15                       | A3                                                                           |
| A5 six Musterbefund route modules | AP16        | AP16                       | A6, A7                                                                       |
| A6 `content/befunde/meta.ts`      | AP16        | AP01                       | none                                                                         |
| A7 MusterbefundPage prop refactor | AP16 / AP25 | AP16                       | A6; keep N13 affordances                                                     |
| A8 ChapterNav hunks               | AP06 / AP24 | AP06                       | D1 for the `sr-only`/token hunk                                              |
| A9 anchor-jump guard              | AP10 / AP24 | AP10                       | A8(b)                                                                        |
| A10 three sitemap routes          | AP09 / AP10 | AP10                       | A4 merged (else three 404-able URLs are advertised)                          |
| A11 `createItemListSchema`        | AP09        | AP01                       | none                                                                         |
| A12 dual origin contract          | AP15        | AP15                       | none (but pointless before A13)                                              |
| A13 Header `kontaktZiel`          | AP06 / AP15 | AP15                       | A12                                                                          |
| A14 mail-subject origin           | AP15        | AP15                       | A12, A13                                                                     |
| A15 phone unification             | AP08 / AP09 | AP01                       | a decision on the single canonical number                                    |
| A16 `bigResult` / `ZAEHLBAR`      | AP16        | AP16                       | A7                                                                           |
| A17 Umbaukonzept docs             | AP15        | AP01                       | reference only — not tracked in-repo                                         |
| A18 chat removal recipe           | AP23        | AP23                       | includes `/api/chat` in `server/server.js`, which `main` does **not** remove |
| A19 CSP HiHuman removal           | AP23        | AP23                       | A18                                                                          |
| A20 event instrumentation         | AP23        | AP23                       | D2, consent architecture settled                                             |
| A21 Panels/Teaser reworks         | AP15        | AP15                       | per-hunk review (gap G3)                                                     |
| B1 error boundaries               | AP10 / AP24 | AP24                       | B2 (or a no-op sink), 6 keys × 10 languages                                  |
| B2 web vitals + reporting         | AP25        | AP25                       | D3 (endpoint decision)                                                       |
| B3 a11y audit gate                | AP24 / AP27 | AP05                       | declare `axe-core`; refresh route list                                       |
| B4 baseline screenshots           | AP27 / AP25 | AP05                       | refresh route list                                                           |
| B5 `usePrefersReducedMotion`      | AP24        | AP01                       | none                                                                         |
| B6 `lib/i18n/format.ts`           | AP08        | AP01                       | none (call-site migration is separate)                                       |
| B7 changelog gate                 | AP27        | AP27                       | re-scope paths away from `src/design-system/`                                |
| B8 CHANGELOG format               | AP27        | AP27                       | reference only                                                               |
| B9 CODEOWNERS                     | AP27        | AP27                       | re-scope paths; real GitHub team                                             |
| B10 metric definitions            | AP23 / AP27 | AP23                       | reference only                                                               |
| B11 UX/persona corpus             | AP05        | AP05                       | reference only                                                               |
| B12 design-system docs            | AP05        | AP05                       | reference only                                                               |
| B13 `.madgerc`                    | AP27        | AP27                       | a real cycle-check gate + `madge` dependency                                 |

---

## 14. Recommended Import Order

1. **AP01 — Layer-0 decisions.** Settle D1 (token gap: add `text.heading` or rename the utility), D2 (tracking adapter over the baseline's `track()`), D3 (monitoring sink or no-op). Nothing else can be sequenced until these three are answered.
2. **AP01 — Dependency-free file imports.** `A1` `epigenetics/tokens.ts`, `A6` `content/befunde/meta.ts`, `A11` `createItemListSchema` + barrel export, `B5` `usePrefersReducedMotion`, `B6` `lib/i18n/format.ts`.
3. **AP05 — Neutral QA tooling.** `B3` a11y audit (declare `axe-core`, refresh routes) and `B4` baseline screenshots (refresh routes). Capture a pre-change screenshot set before any UI work — this is the only cheap regression net available.
4. **AP06 — Shell prerequisites.** `A8` the three `ChapterNav` hunks.
5. **AP10 — Routing/HTTP semantics.** `A9` anchor-jump guard.
6. **AP15 — Epigenetics depth pages.** `A2` → `A3` → `A4` → `A10`, in that order (blueprint §6.1 steps 5–10).
7. **AP16 — Musterbefunde.** `A7` prop refactor (keeping the two baseline affordances) → `A5` six route modules → routes with the catch-all last → `A16` polish.
8. **AP15 — Funnel closure.** `A12` → `A13` → `A14`, strictly in that order.
9. **AP08/AP09 — Data and metadata.** `A15` phone unification across the baseline's own file set.
10. **AP24/AP25 — Resilience and performance.** `B2` monitoring (per D3) → `B1` error boundaries + 6 locale keys × 10 languages.
11. **AP27 — Quality gates.** `B7` re-scoped changelog gate, `B9` re-scoped CODEOWNERS, `B13` if a cycle-check gate is actually wanted.
12. **AP23 — Consent and tracking, last.** `A18` chat removal (including `POST /api/chat`, which no source branch removes) → `A19` CSP HiHuman domains → `A20` event instrumentation on the settled event API.

**Sequencing rule that holds throughout:** every step above is an **edit against the baseline**. `git merge`, `git cherry-pick` and `git checkout <ref> -- <path>` are all unsafe here for any file that exists on both refs, because `main` is older on all shared infrastructure (§11).

---

## 15. Final Positive Import List

**IMPORT (10)** — copy the named file or hunk, adapting only route lists / declared dependencies where noted.

1. `src/components/epigenetics/tokens.ts` — from `main` (`b699435`, `543fe41`), verbatim. _(AP15)_
2. `src/content/befunde/meta.ts` — from `main` (`d0fdf29`), verbatim. _(AP16)_
3. Six route modules `src/pages/musterbefund/*.tsx` — from `main` (`d0fdf29`), verbatim after A7. _(AP16/AP25)_
4. `ChapterNav` hunks — `NavAction.onClick`, `data-chapterbar=""`, `sr-only` switcher label (`543fe41`). _(AP06/AP24)_
5. `ScrollToHash` chapter-bar-ready guard in `src/App.tsx` — hunk only, from `main`. _(AP10/AP24)_
6. `createItemListSchema` + its `seo/index.ts` export (`86fd38d`) — additive only. _(AP09)_
7. `src/hooks/usePrefersReducedMotion.ts` — from `redesign/preview` (`a44ecc5`), verbatim. _(AP24)_
8. `src/lib/i18n/format.ts` — from `redesign/preview` (`a44ecc5`), verbatim. _(AP08)_
9. `scripts/a11y-audit.mjs` + `audit:a11y` script (`707dddf`) — add an explicit `axe-core` devDependency and refresh the route list. _(AP24/AP27)_
10. `scripts/baseline-screenshots.mjs` (`db88eb4`) — refresh the route list. _(AP27/AP25)_

**REIMPLEMENT (15)** — recreate the behaviour against the baseline; do not copy the file.

11. `EpiSubpage.tsx` — adapt `trackEvent` → baseline `track()`, resolve `text-text-heading`. _(AP15)_
12. `EpigeneticsBasicsPage.tsx` / `EpigeneticsEvidencePage.tsx` / `EpigeneticsDocsPage.tsx` — same adaptations. _(AP15)_
13. Three depth-page routes + lazy imports in `src/App.tsx` — hunk-level, never the file. _(AP10)_
14. Three `SITEMAP_ROUTES` entries in `server.ts` — hand-edited; also feeds `KNOWN_PATHS`. _(AP09/AP10)_
15. `MusterbefundPage` prop refactor + `content/befunde/index.ts` split — keeping `LanguageFallbackNotice` and `befund.toTop`. _(AP16/AP25)_
16. `ALWAYS_OPEN` + `bigResult`, and the `ZAEHLBAR` value counter. _(AP16)_
17. Dual origin contract in `ContactForm` (`topic=epigenetik` **or** `source=epigenetics`). _(AP15)_
18. `kontaktZiel` in `Header.tsx` — hunk-level, never the file. _(AP06/AP15)_
19. Origin extraction + `[Epigenetik]` subject in `server/server.js` — hunk-level. _(AP15)_
20. Phone-number unification across the baseline's larger file set (three numbers in circulation). _(AP08/AP09)_
21. Tailwind `text: { heading: '#083358' }` group only — or rename the utility instead. _(AP05)_
22. `src/routing/*` error boundaries + 6 locale keys × 10 languages. _(AP10/AP24)_
23. `src/lib/monitoring/*` — with a decided sink for `/api/monitoring/*`. _(AP25)_
24. `changelog-gate` CI job — re-scoped away from `src/design-system/`. _(AP27)_
25. `.github/CODEOWNERS` — re-scoped paths, real team. _(AP27)_

---

## 16. Final Negative Import List

**DO_NOT_IMPORT (24)**

From `main@d0fdf29`:

1. `server.ts` — in any form (destroys real-404, legacy 301s, German-only 301s, `no-store`, robots de-dup, font preload, `127.0.0.1` bind).
2. `src/components/seo/SEOHead.tsx` — removes `notFound` and `GERMAN_ONLY_PATHS`.
3. `index.html` — re-adds the global German `meta keywords`.
4. `tailwind.config.js` — legacy `gray-900` navy, `accent.on-dark` → teal-300, `success.strong` → emerald-600, drops `'Inter Fallback'`. _(the `text.heading` group excepted)_
5. `src/components/sections/CtaSection.tsx` + `cta_section.*` locale keys — the "garantierte Performance" band (Lock 8).
6. `src/components/layout/Footer.tsx` — renders that band; swaps the logo asset.
7. `src/components/sections/DealPopup.tsx`, `DealHint.tsx` — Lock 9.
8. `src/pages/HomePage.tsx.bak-nopopup`, `src/pages/ContactPage.tsx.bak-nopopup` — committed editor backups.
9. `src/lib/tracking.ts` — direct `dataLayer` push; contradicts Lock 10.
10. `src/components/ui/CookieBanner.tsx` — 73 lines behind the baseline.
11. `src/components/layout/Header.tsx` wholesale — WCAG 44 px targets lost, aria-labels hardcoded to English.
12. The `GermanOnlyPage` removal in `src/App.tsx` — German copy under nine foreign prefixes.
13. The `LanguageFallbackNotice` / `befund.toTop` removals in `MusterbefundPage.tsx`.
14. `src/components/seo/structuredData.ts` wholesale — loses ISO-8601 date normalisation.
15. `src/data/products.ts` — shop catalogue with prices; Lock 9; dead code.
16. `scripts/convert-og-image.js` — duplicate of the baseline's `.mjs`.
17. Any wholesale file/tree import from `main` — it deletes the 17 Sales-Machine sections plus `LegalLayout`, `ImagePlaceholder`, `LanguageFallbackNotice`, `lib/articleMeta.ts` (Lock 1).
18. `main`'s script set as-is — the baseline's `check-color-tokens.mjs`, `check-i18n-home.mjs`, `check-meta-descriptions.mjs`, `i18n/sync-home-from-de.mjs` do not exist there; `check:colors` is wired into `lefthook.yml`.
19. `main`'s `docs/` state — `deploy-preview.md`, `design-system.md`, `backlog.md`, `global-fixes.md`, `migration-map.md` are all absent there.

From `redesign/preview@5673b61`:

20. `src/design-system/**` (29 files incl. `tokens/tokens.css`, `tokens/tokens.ts`) — the alternative art direction (Locks 1–3).
21. `src/pages/StyleguidePage.tsx`, `e2e/styleguide-visual.spec.ts`, the `Visual regression (Pattern Library)` CI step — inseparable from #20.
22. `src/components/sections/home/*` (9 files), `CtaBand.tsx`, `CtaSection.tsx`, `ContactChannels.tsx`, `public/images/*` — alternative home composition and a second guarantee-CTA variant.
23. `public/sitemap.xml` — shadows the baseline's dynamic, hreflang-complete sitemap.
24. `src/lib/flags.ts` (placeholder-only stub), `.archon/**` workflows, `EXECUTION-PLAN.md`, `REFACTOR-LOG.md`, `docs/RELAUNCH-*.md`, `docs/NEWLOOK-HOME.md` — dead code and a competing relaunch plan.

From `feat/contact-joyful@ab373a3`:

_(no separate entry — the whole branch is `REFERENCE_ONLY`; see §9.)_

---

## 17. Open Evidence Gaps

Factual questions this pass could not resolve from the repository. No product decisions are invented here.

**G1 — `/api/monitoring/*` has no server-side counterpart anywhere.**
`src/lib/monitoring/report.ts` beacons to `/api/monitoring/client-error` and `/api/monitoring/web-vitals`. Neither path is handled in `server.ts` or `server/server.js` on `961f65d`, `d0fdf29` or `5673b61`; they fall into the generic `/api/` proxy (`server.ts:381` on `5673b61`). The intended collector — the file's own comment says it lives "außerhalb dieses Repos, hinter dem `/api`-Proxy" — cannot be located from this repository. B1/B2 cannot be scheduled until this is answered outside the repo.

**G2 — Which phone number is canonical.**
`main@475fefd` unified on one number. The baseline carries three (`+49 152 2858 0999` 13×, `+49 151 75011699` 10× + 5× unspaced, `+49 1515 9878599` 1×). The repository shows the unification but not the authority for it; A15 needs that confirmation before a data pass.

**G3 — `EpigeneticsPanels.tsx` and `EpigeneticsTeaserSection.tsx` were not diffed hunk-by-hunk.**
Both files exist on the baseline **and** on `main` and were changed independently on both lines (`b699435`, `86fd38d` on `main`; `54eb730`, `bd9c67c`, `baf6e3d` on the baseline). A per-hunk comparison is required before classifying anything inside them; A21 is parked as `DEFER` for that reason, not because the work is unwanted.

**G4 — The six missing English Epigenetics PDFs.**
`projektverzeichnis/10-befunde.md` §8 records 17 German vs 9 English PDFs under `public/downloads/epigenetics/`. `EpigeneticsDocsPage` (A3, step 8) will therefore render an asymmetric resources page. Whether the English PDFs exist outside the repository is not determinable here.

**G5 — Whether `redesign/preview`'s devDependency additions are wanted independently.**
`5673b61` adds `@testing-library/{dom,jest-dom,react}`, `jsdom`, `eslint-plugin-boundaries`, `eslint-plugin-import`, `eslint-plugin-jsx-a11y`. Some (testing-library, jsx-a11y) are plausibly useful to AP27/AP24 on their own; `eslint-plugin-boundaries` exists to enforce the excluded `src/design-system/` layering. They were not individually evaluated in this pass. Note also the standing environment fact from the repository memory `sandbox-runtime-gates-blocked`: jsdom/`npm test` is blocked in this sandbox while Playwright/Chromium runs — so vitest-based additions cannot be verified locally here.

**G6 — Baseline `casestudies` namespace.**
`public/locales/<lang>/casestudies.json` ships in all ten languages but is registered in no `NAMESPACES` entry of `src/i18n.ts` (`projektverzeichnis/10-befunde.md` §3). This is a pre-existing baseline condition, unaffected by any import above, and is recorded here only so it is not mistaken for import fallout.

---

## 18. Final Classification

## RECONCILIATION_READY_WITH_WARNINGS

**Why READY.** The reconciliation itself is fully determined. Every Master-Scope candidate named in the brief was located, read and classified against repository evidence:

- The Epigenetics depth pages exist as three concrete files plus a shared frame and a token module, and — critically — **their entire i18n contract is already satisfied by the baseline.** All twelve keys `EpiSubpage` reads were verified present in `961f65d:public/locales/de/epigenetics.json`, and `common:read_more` at line 66. All nine of its component/lib dependencies exist on the baseline, including `Breadcrumbs` with the `variant="dark"` it needs. This is the difference between an import and a rewrite, and it lands on the import side.
- `EpiSubpage` is reusable without dragging in unrelated layout or art direction: its only two blockers are a one-token Tailwind gap and one function-name mismatch, both named precisely in §6.1 steps 1–2.
- The Musterbefund route modules and `content/befunde/meta.ts` are new, self-contained files whose purpose is measured, not asserted (287 KB chunk to display 24 KB).
- The negative map is complete and specific: 24 items with commit, file, reason and coupling.
- Ordering is closed: §14 gives a 12-step sequence in which every dependency edge from §10 is satisfied before its consumer.

**Why WITH WARNINGS.** Four conditions must travel with this map:

1. **No commit is `SAFE_TO_CHERRY_PICK`.** All 17 evaluated commits are `PARTIAL_ONLY` or `DO_NOT_CHERRY_PICK` (§12). Because `main` is older than the baseline on `server.ts`, `SEOHead.tsx`, `index.html`, `tailwind.config.js`, `Header.tsx`, `CookieBanner.tsx` and `lib/tracking.ts`, **any** git-level transfer of a shared file silently regresses hardening the locks require preserved. Every recommended change is a hand-authored edit against the baseline.
2. **Six candidates sit inside files whose `main` version is forbidden** — A4/A9 in `App.tsx`, A10 in `server.ts`, A11/A15 in `structuredData.ts`, A13 in `Header.tsx`, A7/A16 in `MusterbefundPage.tsx`, A12 in `ContactForm.tsx`. These are the highest-probability failure points for a coding agent working from a commit list rather than this map.
3. **Three Layer-0 decisions block the sequence** (§10): the Tailwind token gap, the tracking adapter over the baseline's consent-gated `track()`, and the missing `/api/monitoring/*` sink (gap G1). The first two are internal and answerable immediately; the third depends on information outside this repository.
4. **The working tree is dirty and was left that way.** `src/pages/EpigeneticsPage.tsx` still carries the partial replay of `e21a6e5` described in §3. It is not part of the baseline, not part of any import above, and — per the task constraints — was neither resolved nor discarded. Whoever executes this map must decide it separately, before touching `EpigeneticsPage.tsx` for A2–A4.

None of these blocks the work. All of them change how it must be executed.

---

_Produced by read-only inspection on 2026-08-21. HEAD unchanged, no branch checked out, no file staged, committed, merged, rebased, cherry-picked or modified. No secrets or environment-variable values are reproduced._

## 19. Nachtrag — `main`-Commits nach `d0fdf29` (2026-08-25)

Diese Map wurde gegen **`main@d0fdf29`** erstellt. Danach ist auf `main` genau ein weiterer Commit
entstanden. Er ist damit der einzige `main`-Inhalt, der **nie** durch die Reconciliation gelaufen ist,
und wird hier geführt, damit er nicht verlorengeht.

### A26 — `f2d5da4` „Future Forum Berlin 2026: Event-Seite mit Anmeldung"

| Feld | Inhalt |
|---|---|
| **Quelle** | `archive/main-pre-relaunch @ f2d5da4` (auf `origin` gesichert, zusätzlich Tag `backup/main-f2d5da4`) |
| **Autor/Datum** | PhillHH, 2026-08-24 |
| **Umfang** | 10 × `public/locales/*/events.json` · `public/og-future-forum-berlin.jpg` (neu, 105 627 Bytes) · `scripts/prerender.mjs` (+1) · `server.ts` (+1) |
| **Klassifikation** | **`PARTIAL_ONLY`** — wie alle 17 in §12 bewerteten Commits. Kein git-seitiger Transfer. |
| **Gemessen (2026-08-25)** | `git cherry-pick -n f2d5da4` gegen den Relaunch-Stand `1e40573` → **Konflikt in 13 Dateien**: die zehn `events.json`, dazu `src/data/events.ts`, `src/pages/EventsPage.tsx`, `src/pages/HomePage.tsx`. Ursache: die Event-Datenmodelle beider Linien sind unabhängig divergiert — der Relaunch führt ISO-Datumsfelder mit `splitEventsByDate` (`CONTENT-ASSET-CONTRACT.md` §5.5 nennt `events.ts` das „sauberste Modell im Repo"), `main` nicht. |
| **Status** | **OFFEN — nicht übernommen.** Der Inhalt ist auf `main` entstanden und beim Zusammenführen der Linien bewusst **nicht** mitgewandert (Merge `e9e32be`, Strategie `ours`). |
| **Owner** | **AP18** (Events) — Datenmodell, Zeitlogik, Archiv |
| **Vorgehen** | Handgeschriebene Übernahme gegen das Relaunch-Event-Modell, **kein** `cherry-pick` und **kein** `git checkout f2d5da4 -- <pfad>`: `server.ts`, `HomePage.tsx` und `prerender.mjs` fallen unter §12 („`main` ist auf diesen Dateien älter als die Baseline"). Übernehmbar sind die fachlichen Event-Daten und das OG-Bild; die Trägerdateien werden neu geschrieben. |
| **Risiko beim Nichtstun** | Eine real gepflegte Veranstaltungsseite fehlt im Relaunch. Kein Launch-Gate ist davon betroffen, aber es ist echte, verlorene Redaktionsarbeit, wenn sie niemand nachzieht. |

**Warum das hier steht und nicht in §15/§16:** Jene Listen sind gegen `d0fdf29` geschlossen und werden
nicht rückwirkend umgeschrieben (§18 hält den Stand dieser Erhebung fest). `A26` ist ein Nachtrag mit
eigenem Erhebungsdatum.

